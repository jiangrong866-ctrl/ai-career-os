services:
  ai-career-os:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
      - ./state:/app/state
      - ./queue:/app/queue
      - ./logs:/app/logs

